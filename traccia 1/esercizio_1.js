// Traccia 1

                            
// Scrivi un programma che dato un numero, ad esempio let num = 2, stampi la rispettiva tabellina corrispondente (da 1 a 10).

// Prova sia utilizzando il ciclo while sia il ciclo for.

let num = 3;
let multiply = 0;

while(multiply < 10) {
    multiply++;
    let total = num*multiply
    console.log(total)
}

// for(let num=3; let multiply=0; let total=num*multiply; multiply<10; multiply++) {
//     console.log(total)
// }
